{
  "name": "notesapp-webapplication",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vite --host 0.0.0.0 --port ${REACT_APP_PORT:-3000}",
    "dev:backend": "python -m uvicorn backend.main:app --host 0.0.0.0 --port ${BACKEND_PORT:-5179} --reload",
    "dev:all": "concurrently -k -n backend,frontend -c blue,green \"npm:dev:backend\" \"npm:dev\"",
    "build": "npx vite build",
    "preview": "npx vite preview --host 0.0.0.0 --port ${REACT_APP_PORT:-3000}",
    "preview:all": "concurrently -k -n backend,preview -c blue,green \"npm:dev:backend\" \"npm:preview\"",
    "healthz": "node -e \"const p=(process.env.REACT_APP_PORT||process.env.PORT||3000); fetch('http://localhost:'+p+'/healthz').then(r=>{console.log('status', r.status); process.exit(r.ok?0:1)}).catch(e=>{console.error(e); process.exit(2)})\"",
    "start": "npx vite --host 0.0.0.0 --port ${REACT_APP_PORT:-3000}"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^8.2.2",
    "vite": "^5.4.8"
  }
}
